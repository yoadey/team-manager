{{- if .Values.database.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "team-manager.fullname" . }}-database
  labels:
    {{- include "team-manager.labels" . | nindent 4 }}
type: Opaque
data:
  DATABASE_URL: {{ .Values.database.secret.url | b64enc }}
{{- end }}
