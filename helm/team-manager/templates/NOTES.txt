{{ include "team-manager.fullname" . }} has been deployed.

{{- if not .Values.image.tag }}

WARNING: image.tag is not set, so the backend image resolves to
Chart.appVersion ("{{ .Chart.AppVersion }}") — a value hand-set in
Chart.yaml that nothing in the release process bumps to match the
latest published tag. .github/workflows/release.yml publishes images
tagged by the actual released version (e.g. "1.2.3", the v stripped),
never "{{ .Chart.AppVersion }}" unless that happens to coincidentally be
a real release. Set image.tag explicitly to a real release tag, e.g.:

  helm upgrade --install {{ .Release.Name }} . --set image.tag=1.2.3

or you risk an ImagePullBackOff against a tag GHCR never published.
{{- end }}
{{- if .Values.ingress.enabled }}

Ingress host(s):
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}
{{- end }}

{{- if and .Values.ingress.enabled (not .Values.server.trustedProxyCidrs) }}

WARNING: ingress is enabled but server.trustedProxyCidrs is not set.

Every request reaches the backend proxied through your ingress controller
({{ .Values.ingress.className | default "nginx" }}), so without
trustedProxyCidrs every real client appears to share the ingress
controller's own IP address for the global rate limiter (rateLimitRps)
and, more importantly, the login brute-force limiter
(loginRateLimitPerMin) — see docs/operations.md's "Rate limiting"
section for the full explanation.

Set server.trustedProxyCidrs to your cluster's ingress-controller pod CIDR
(or load balancer's known egress range) via a values override, e.g.:

  helm upgrade --install {{ .Release.Name }} . \
    --set server.trustedProxyCidrs[0]=10.244.0.0/16

The correct CIDR is cluster/CNI-specific (varies by Kubernetes
distribution and networking plugin) and cannot be safely defaulted by
this chart — a too-broad value would let clients on the same network
spoof X-Forwarded-For and bypass rate limiting entirely, which is worse
than leaving it unset.
{{- end }}

{{- if and .Values.session.cookie.secure (not .Values.metrics.allowOpen) }}

REMINDER: session.cookie.secure is true, so the backend hard-fails startup
(exit 1, every replica crash-loops) unless your metrics.secret Secret
({{ include "team-manager.secretName" (list $ "metrics" .Values.metrics.secret) | default "metrics.secret.existingSecret / metrics.secret.create" }})
has a non-empty METRICS_TOKEN key — this chart cannot verify that from
here, since it only sees the Secret's name, not its contents. If you
haven't set one, either populate METRICS_TOKEN in that Secret, or set
metrics.allowOpen=true if /metrics is already restricted at the network
layer and you accept it being unauthenticated.
{{- end }}

{{- if and .Values.session.cookie.secure (not .Values.s3.endpoint) }}

WARNING: session.cookie.secure is true but s3.endpoint is not set, so the
backend hard-fails startup (exit 1, every replica crash-loops) — image
uploads (team/user photos, team logos) require object storage to be
configured, unlike METRICS_TOKEN above this chart CAN see this from here.
Set s3.endpoint/s3.region/s3.bucket/s3.usePathStyle via a values override,
and either populate s3.secret.existingSecret (S3_ACCESS_KEY_ID/
S3_SECRET_ACCESS_KEY) or set s3.secret.create=true with the plaintext
fields — see docs/operations.md's "Object storage (image uploads)" section.
{{- end }}

{{- if and .Values.session.cookie.secure (or (not .Values.smtp.host) (not .Values.smtp.fromAddress)) }}

WARNING: session.cookie.secure is true but smtp.host/smtp.fromAddress are
not both set, so the backend hard-fails startup (exit 1, every replica
crash-loops, including the migrate initContainer since config.Load() runs
before --migrate-only is handled) — self-registration verification email
requires SMTP to be configured. Set smtp.host/smtp.port/smtp.fromAddress via
a values override, and optionally populate smtp.secret.existingSecret
(SMTP_USERNAME/SMTP_PASSWORD, both may be blank for an open relay) or set
smtp.secret.create=true with the plaintext fields — see
docs/operations.md's "Outbound email (SMTP)" section.
{{- end }}

{{- if and .Values.backup.enabled .Values.backup.s3.enabled }}

REMINDER: backup.retentionDays ({{ .Values.backup.retentionDays }}) is
informational only -- nothing in this chart enforces it. The backup
CronJob uploads one dump per successful run to
s3://{{ .Values.backup.s3.bucket }}/{{ .Values.backup.s3.prefix }}/ and
never deletes old objects itself. Without a matching S3 (or S3-compatible)
bucket lifecycle rule configured separately, backups accumulate in that
bucket indefinitely -- unbounded storage cost, and a data-minimization/
compliance risk for backups containing personal data retained far longer
than any documented policy.
{{- end }}
